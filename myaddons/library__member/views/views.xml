<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="library__member.list">
      <field name="name">Library_Member list</field>
      <field name="model">library__member.library__member</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="library__member.action_window">
      <field name="name">Library_Member window</field>
      <field name="res_model">library__member.library__member</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="library__member.action_server">
      <field name="name">Library_Member server</field>
      <field name="model_id" ref="model_library__member_library__member"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Library_Member" id="library__member.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="library__member.menu_1" parent="library__member.menu_root"/>
    <menuitem name="Menu 2" id="library__member.menu_2" parent="library__member.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="library__member.menu_1_list" parent="library__member.menu_1"
              action="library__member.action_window"/>
    <menuitem name="Server to list" id="library__member" parent="library__member.menu_2"
              action="library__member.action_server"/>
    -->
  </data>
</odoo>